const logger = require("../logger");
// const git = require("../git");
// const config = require("../config").config.shouldMergeWithOtherBranches;

const shouldMergeWithOtherBranches = () => {
  logger.log("TODO shouldMergeWithOtherBranches");
};

module.exports = { shouldMergeWithOtherBranches };
